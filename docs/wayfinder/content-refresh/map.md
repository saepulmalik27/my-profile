---
label: wayfinder:map
tracker: local-markdown
---

# Portfolio Content Refresh (Wayfinder Map)

## Destination

Finalized, copy-ready content for the website: project deep-dives (challenges tackled, problems solved) for **Inspigo For Business**, **Inspigo AI**, ten more not-yet-documented Inspigo-era projects (n8n workflow/security automation, CI/CD, Storybook docs, AWS S3/Amplify/Vercel infra, CMS, admin analytics dashboard, video player, GTM/GA/MoEngage tracking, SEO, Agora+Firebase realtime chat), Praweda's **Shipyard Project Management System**, Praweda's **Trucking Management Application**, and Indocyber's **trainee project**; a refreshed **Skills & Techstack** list; and a decided site placement plus captured content for three sections that don't exist yet — **Currently Learning**, **Personal Projects**, **Courses**. Everything is recorded as ticket Answers, ready to transcribe into `apps/portfolio/content/*.ts`, `content/projects/*.mdx`, and whatever new section ticket 07 lands on.

## Notes

- **Domain:** `apps/portfolio`'s content layer only (`content/*.ts`, `content/projects/*.mdx`). Per standing decision below, **no code/file edits happen inside this map** — every ticket produces a decision (content facts), transcription into the actual files is separate execution afterward.
- **Tracker:** no issue tracker configured for this repo — same **local-markdown fallback** used by the (closed) Portfolio Revamp map: this file is the map, each `tickets/NNN-slug.md` is a child ticket, claimed via `assignee:`, blocked via `blocked_by: [ids]` in frontmatter. Frontier = open, unassigned tickets with all `blocked_by` ids closed.
- **Plan, don't do:** confirmed with the user — this map stops at decisions. Pasting finalized content into `content/career.ts`, `content/skills.ts`, `content/projects/*.mdx`, and any new schema fields is out of this map, tracked under Not yet specified.
- **Skills to consult:** `/domain-modeling` for ticket 07 (deciding where the three new sections live in the site IA) and any schema-shape questions that come up while grilling. `/grill-with-docs` is reserved for explicit user invocation and can't be called by the agent (learned during the Portfolio Revamp map) — grilling tickets are worked as a live conversation in-session.
- **Relationship to the Portfolio Revamp map:** that map locked the site's page IA (Home/Career/Skills/About routes), visual theme, and the About page's physics-era milestone — none of that is revisited here. This map only refines/expands the _facts_ that feed Career and Skills & Showcase, plus decides where the three net-new sections go.
- **Ties to `docs/recruiter-feedback.md`:** that review flagged three gaps this map is a direct chance to close — no quantified impact in achievements, no visuals/evidence for case studies, and no visible personal-project/GitHub proof. Grilling tickets should actively ask for numbers, screenshot/demo availability, and concrete links, not just prose.
- **User-directed working order:** Inspigo For Business (01) → Inspigo AI (02) → the ten newly-discovered Inspigo projects (11–20, any order within the group) → Praweda Shipyard (03) → Praweda Trucking (04) → Indocyber (05) → Skills & Techstack (06) → IA placement for the three new sections (07) → Currently Learning (08) → Personal Projects (09) → Courses (10). Ticket ids aren't sequential with this order — 11–20 were added mid-session when the scope widened (see below) — this line is the source of truth for working order, not the filenames.
- **Scope widened mid-charting:** after the first pass, the user flagged that many Inspigo-era projects existed beyond the two already in `content/projects/*.mdx` (Inspigo For Business, Inspigo AI). Confirmed via a quick scoping round: all ten are Inspigo-era (2021–present), the n8n/security-automation one (ticket 11) is official Inspigo work (not a personal project, so it stays out of ticket 09), and each gets its own ticket (one-per-item, matching the depth given to the original two) rather than being bundled by theme.
- **Scope widened again while grilling ticket 11:** what looked like one "n8n automation" item turned out to be two unrelated projects — the pentest automation workflow (kept in ticket 11) and a standalone **AI call-center agent copilot** POC (real-time transcription + AI checklist + rubric-based auto-evaluation), split into new [ticket 21](tickets/21-inspigo-ai-callcenter-copilot-detail.md). Worth staying alert for more of these hiding inside the other bundled tickets (12–20) as they get grilled.
- **Answer/ticket mismatch caught while grilling ticket 15:** the answer given for "CMS (forms, media upload)" (ticket 15) actually described the admin panel — member/org management, stats, learning-path — which is ticket 16's territory, and the only evidence folder present was `admin-dashboard/` (no CMS-specific one). Moved that answer to ticket 16 and closed it; reopened ticket 15 to capture the actual forms/media-upload story.

## Decisions so far

- [Inspigo For Business: capture project detail, challenges, and problems solved](tickets/01-inspigo-for-business-detail.md) — Before: hardcoded per-client builds, unsustainable once several clients signed close together with their own revisions. Hard problem: config drives component composition/layout, not just theme/copy. Solo-designed the architecture with 1 backend engineer. ~350 client orgs, ~100 users/client (~35k end users). Screenshots at `apps/portfolio/public/project/ifb/`.
- [Inspigo AI: capture project detail, challenges, and problems solved](tickets/02-inspigo-ai-detail.md) — RAG via CMS upload → S3 → Pinecone (chunking/metadata) with OpenAI embeddings; OpenAI primary model, Bedrock fallback; WebSocket (legacy roleplay) + SSE (newer) streaming. Hard problem: scaling to concurrent users. Owned frontend/streaming UX + markdown rendering + CMS interface. 5-person team, peak thousands of chats/day. Screenshots at `apps/portfolio/public/project/inspigo-ai/`.
- [Inspigo — n8n pentest automation workflow: capture project detail, challenges, and problems solved](tickets/11-inspigo-n8n-automation-detail.md) — Weekly scheduled scan of production: SCA/SAST (Semgrep, pnpm audit) + DAST (Nuclei, OWASP ZAP), AI-formatted (OpenAI) into an OWASP-standard report, converted to PDF, delivered to Rocket.Chat. Requested by the engineering manager. Hard problems: no authenticated-route scanning yet (headers only), and microservice sprawl multiplies scan surface. Screenshot at `apps/portfolio/public/project/automation/pentest.png`. Split off the AI call-center copilot into ticket 21.
- [Inspigo — AI call-center agent copilot: capture project detail, challenges, and problems solved](tickets/21-inspigo-ai-callcenter-copilot-detail.md) — OpenAI live transcription + per-case knowledge table drives checklist/evaluation. Hard problems: AI-vs-human scoring margin of error >5 on subjective rubric dimensions; transcription accuracy (agent/customer voice separation, background noise). Product team's idea, built with 2 product-team members. Covers all ~11 agents; automates 100% session scoring vs. the prior ~10%-sample manual QA. Screenshot at `apps/portfolio/public/project/automation/copilot.png`.
- [Inspigo — CI/CD using GitHub Actions: capture project detail, challenges, and problems solved](tickets/12-inspigo-cicd-detail.md) — Security audit, typecheck & test, and build+deploy pipelines; no CI existed before, still only on a handful of projects. Deliberately scoped to run on the production branch + version tags only, not every push. Solo-built, brainstormed with AI. Impact: faster builds, push-to-deploy instead of manual `pm2` SSH work, automatic audit/test on serverless targets. No dedicated evidence artifact yet (the referenced `workflow.yml` turned out to belong to ticket 11).
- [Inspigo — project documentation using Storybook: capture project detail, challenges, and problems solved](tickets/13-inspigo-storybook-detail.md) — Documents the shared cross-app design system/component library only. Hard problem (unsolved): keeping stories in sync with real components — maintenance lost out, Storybook here has since gone stale/unmaintained. Own initiative, solo. Covers nearly all cross-app shared components, especially small primitives. Not presentable outside Inspigo. Site copy should frame this as past-tense ("built X"), not as an ongoing practice.
- [Inspigo — AWS S3 media upload, Amplify, and Vercel deployment: capture project detail, challenges, and problems solved](tickets/14-inspigo-cloud-infra-detail.md) — S3 upload via presigned URLs (direct) and a backend-proxied path with a Lambda image converter/optimizer. Amplify for hosting/deploy; all projects migrated off Vercel onto Amplify. Hard problem: Amplify deploys take 6–10 min with manual cache management, worse than Vercel's automatic caching. Collective initiative (EM + team). Impact: better media organization than DB-stored media, easier image optimization. Screenshots at `apps/portfolio/public/project/aws/`.

- [Inspigo — Admin dashboard (charts, monitoring, statistics for clients): capture project detail, challenges, and problems solved](tickets/16-inspigo-admin-dashboard-detail.md) — Custom admin panel covering member management, org management, member stats, and a learning-path module; built on RHF+Zod and Recharts. Hard problem: permissions/roles, since the backend authz model is a JWT-attached scope system rather than a role table. Built with the product team + a dedicated backend engineer. Adoption: went from nothing to near-universal — almost every client org uses it, ~2 admins/org. Screenshot at `apps/portfolio/public/project/admin-dashboard/`.

## Not yet specified

- **Transcribing finalized content** into `apps/portfolio/content/career.ts`, `content/skills.ts`, `content/projects/*.mdx`, and whatever new file/fields ticket 07 decides on — pure execution once every ticket below is closed.
- **Producing case-study visuals** (screenshots, GIFs, or a short demo video) referenced by `docs/recruiter-feedback.md` ticket 5 — asset creation is separate from writing the case-study text captured here.
- **Final prose polish pass** across all captured content, once everything is transcribed — copyediting, not a decision.

## Out of scope

- The site's page IA (routes, nav, 3D home scene, visual theme) and the About page's physics-era milestone — already decided by the closed Portfolio Revamp map (`docs/wayfinder/portfolio-revamp/map.md`); not reopened here.
