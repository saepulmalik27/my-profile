---
id: '05'
title: 'Indocyber: capture trainee project detail, challenges, and problems solved'
type: grilling
status: closed
assignee: saepulalmalik@gmail.com
blocked_by: []
---

## Question

`content/career.ts` currently lists Indocyber (2018, trainee) as: trained in C#, Java, SQL Server, Java Spring, MVC; built a Library Management System as a final project. This is the shortest/earliest entry — decide how much depth it deserves:

- Is the Library Management System worth a real problem/challenge writeup (own MDX case study, matching the other three), or is a couple of honest sentences enough given it's a training project rather than production work? Recruiter lens: this entry mainly needs to establish "here's the foundation the full-stack years built on" — it doesn't need to carry the same evidentiary weight as paid production work.
- If it gets real detail: what did the Library Management System actually do (catalog, checkout/return, member management, something else), and was there any specific technical challenge worth naming?
- What specifically was learned in the C#/Java/SQL Server/Spring/MVC training that mattered later — is there a throughline to the PHP/Vue.js work that immediately followed at Praweda?
- Confirm: no numbers/metrics expected here (training project), but confirm scope/duration of the trainee program itself (how many weeks/months, structured curriculum or self-directed).

## Answer

**Depth decision:** Light-touch entry, not a full case study — training curriculum covered C#, Java, SQL Server, and JavaScript broadly across the program.

**Library Management System:** Built using MVC concepts, implemented in both .NET and Java Spring.

**Throughline:** Learning MVC architecture here (across two different stacks) is the throughline into the PHP/Vue.js work that followed at Praweda — the same intake→process→output mental model carried over even though the language/framework changed.

**Scope/duration:** Not specified beyond the above — no further detail on program length or curriculum structure was given.

**Evidence:** None — training project, no numbers/metrics expected or available.
