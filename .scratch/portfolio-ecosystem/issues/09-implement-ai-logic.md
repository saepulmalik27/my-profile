# 09 — Implement AI Chatbot Logic (API Route)

**What to build:** An API route in `apps/portfolio` using Vercel AI SDK that responds to queries with static mock AI responses (or direct OpenAI integration if keys are provided).

**Blocked by:** 06 — Build the Home Bento Grid Shell (Portfolio)

**Status:** ready-for-agent

- [x] Create `/api/chat` endpoint using Vercel AI SDK.
- [x] Configure a system prompt with the developer's CV data.
- [x] Test the endpoint to ensure streaming works.
