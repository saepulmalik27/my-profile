# 01 — Initialize Monorepo Foundation

**What to build:** A working Turborepo setup using pnpm with a placeholder `apps/portfolio` (Next.js) and `packages/ui`.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [x] Run `npx create-turbo` or manually configure pnpm workspaces.
- [x] Ensure `apps/portfolio` and `packages/ui` exist and can be built.
