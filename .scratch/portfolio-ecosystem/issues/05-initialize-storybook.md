# 05 — Initialize Storybook App

**What to build:** A running `apps/storybook` instance that renders the Button component from `packages/ui`, proving the design system documentation works.

**Blocked by:** 04 — Setup UI Package (Tailwind & Shadcn)

**Status:** ready-for-agent

- [x] Setup Storybook in `apps/storybook`.
- [x] Configure it to read from `packages/ui`.
- [x] Render the Button component in a story.
