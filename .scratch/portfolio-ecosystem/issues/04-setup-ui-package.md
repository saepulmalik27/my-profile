# 04 — Setup UI Package (Tailwind & Shadcn)

**What to build:** `packages/ui` configured with Tailwind CSS and Shadcn UI (Bold & Colorful theme tokens). The Next.js portfolio can consume a basic Button component from `packages/ui`.

**Blocked by:** 03 — Setup GitHub Actions (CI/CD Pipeline)

**Status:** ready-for-agent

- [x] Configure Tailwind CSS in `packages/ui`.
- [x] Initialize Shadcn UI in `packages/ui`.
- [x] Export a basic Button component.
- [x] Import and render the Button in `apps/portfolio`.
