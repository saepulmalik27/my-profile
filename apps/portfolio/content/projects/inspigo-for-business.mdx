---
title: 'Inspigo For Business: A Customizable Enterprise Learning Platform'
company: 'PT Inspigo Inovasi Indonesia'
period: '2021–Present'
summary: 'Dynamic, per-client UI customization for a B2B enterprise learning platform — configurable without a dedicated build per client.'
tags: ['Next.js', 'React', 'Design Systems', 'State Management']
---

## Problem

Before this system existed, per-client customization was hardcoded — a
separate build or branch per enterprise deal. That got heavy fast once
several clients signed in a short window, each wanting revisions on top
of their own hardcoded variant: no shared system, so every client's
change was isolated, repeated work.

## Architecture

The platform is built on Next.js and React, with branding and layout
treated as configuration data rather than code paths. The hard part
wasn't theming — colors and copy are the easy 80%. The real architecture
problem was that config also has to drive component _composition and
layout_: which components render and how they're arranged per tenant,
not just how they're styled.

## Execution

Designed and built the config-driven architecture from scratch, working
as a two-person team (frontend lead + one backend engineer). The
customization layer covers what enterprise clients actually care about
seeing reflected back at them — branding, content organization, and
structural layout adjustments — all resolved from per-tenant
configuration data.

![IFB dynamic UI configuration](/project/ifb/ifb-dyanmic-ui.png)

![IFB per-tenant layout](/project/ifb/ifb-dynamic-ui-2.png)

## Impact

New enterprise clients can be onboarded through configuration rather
than a dedicated development cycle per client — the platform scales with
the sales pipeline instead of the engineering roadmap. Now running
~350 client organizations, averaging ~100 users each (~35,000 end users
total).
